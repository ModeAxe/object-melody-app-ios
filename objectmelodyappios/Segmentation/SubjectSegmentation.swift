import UIKit
import VisionKit

struct SubjectSegmentation: SegmentationStrategy {
    let name = "VisionKit Subject Segmentation"
    
    func segmentObject(in image: UIImage, completion: @escaping (UIImage?) -> Void) {
        return 
    }
} 
